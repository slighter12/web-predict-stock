[project]
name = "quant-platform"
version = "0.1.0"
description = "Personal Quantitative Research Platform"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pandas",
    "vectorbt",
    "xgboost",
    "statsmodels",
    "psycopg2-binary",
]
